
// This file replaces firebase.ts with a mock configuration
export const mockConfig = {
  apiVersion: '1.0.0',
  environment: 'development',
  mockEnabled: true
};

// Placeholder for the db and auth references that would normally come from Firebase
export const db = {};
export const auth = {};
